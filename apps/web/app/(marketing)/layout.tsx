// ===========================================
// OpoMetrics - Marketing Layout
// ===========================================

export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
